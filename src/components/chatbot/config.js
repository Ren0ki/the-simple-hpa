import { createChatBotMessage } from "react-chatbot-kit";

const config = {

    botName: "Proteome",
    initialMessages: [createChatBotMessage("Interested in cancer genomics? Let's chat!")],
};

export default config;